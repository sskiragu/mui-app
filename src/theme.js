import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette:{
        primary: {
            main: "#ff8c00",
            light: "skyblue"
        },
        secondary: {
            main: "#ffff00"
        },
        otherColor: {
            main: "#999"
        } 
    }
})